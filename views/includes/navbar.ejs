<nav class="navbar sticky top-0 z-50 h-20 bg-white border-b border-gray-300">
  <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
    <div class="relative flex h-20 items-center justify-between">
      
      <!-- Mobile Menu Button (Left) -->
      <div class="absolute inset-y-0 left-0 flex items-center sm:hidden">
        <button id="mobile-menu-btn" type="button" class="relative inline-flex items-center justify-center rounded-md p-2 text-gray-500 hover:bg-gray-300 focus:outline-none">
          <span class="sr-only">Open main menu</span>
          <svg id="hamburger-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" class="block size-6">
            <path d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" stroke-linecap="round" stroke-linejoin="round" />
          </svg>
          <svg id="cross-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" class="hidden size-6">
            <path d="M6 18 18 6M6 6l12 12" stroke-linecap="round" stroke-linejoin="round" />
          </svg>
        </button>
      </div>

      <!-- Left Section: Logo & Explore -->
      <div class="flex flex-1 items-center justify-start">
        <div class="flex text-3xl shrink-0 items-center">
          <a href="/listings"><i class="fa-solid fa-hotel text-3xl text-[#fe424fb4]"></i></a>
        </div>
        <div class="hidden sm:ml-6 sm:block">
          <div class="flex space-x-4">
            <a href="/listings" class="rounded-md px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-300">Explore</a>
          </div>
        </div>
      </div>

      <!-- Center Section: Search Bar -->
   <div class="flex-1 flex justify-center items-center px-2">
    <form class="w-full max-w-md flex gap-2" action="/listings" method="GET">
        <label for="search" class="sr-only">Search</label>
        
        <div class="relative flex-grow">
            <div class="absolute inset-y-0 start-0 flex items-center ps-3.5 pointer-events-none">
                <svg class="w-4 h-4 text-gray-400" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                    <path stroke="currentColor" stroke-linecap="round" stroke-width="2" d="m21 21-3.5-3.5M17 10a7 7 0 1 1-14 0 7 7 0 0 1 14 0Z"/>
                </svg>
            </div>
            <input type="search" id="search" name="search"
                   class="block w-full p-2.5 ps-10 text-sm text-gray-900 border border-gray-200 rounded-3xl bg-gray-50 focus:ring-blue-500 focus:border-blue-500 shadow-sm transition-all" 
                   placeholder="Search destinations" required />
        </div>

        <button type="submit" 
                class="bg-blue-600 hover:bg-blue-700 text-white font-medium text-sm px-5 py-2.5 rounded-3xl whitespace-nowrap shadow-sm">
                <i class="fa-solid fa-magnifying-glass mr-2"></i>
            Search
        </button>
    </form>
</div>

      <!-- Right Section: Auth Links -->
      <div class="flex flex-1 items-center justify-end">
        <div class="hidden sm:flex sm:items-center sm:space-x-4">
          <a href="/listings/new" class="rounded-md px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-300">Add new Listing</a>
          <% if(!currentUser){ %>
            <a href="/signup" class="rounded-md px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-300"><b>Sign Up</b></a>
            <a href="/login" class="rounded-md px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-300"><b>Log In</b></a>
          <% } if(currentUser){ %>
            <a href="/logout" class="rounded-md px-3 py-2 text-sm font-medium text-gray-700 hover:bg-gray-300"><b>Log Out</b></a>
          <% } %>
        </div>
      </div>

    </div>
  </div>

  <!-- Mobile Menu Dropdown -->
  <div id="mobile-menu" class="hidden sm:hidden bg-gray-100 pb-3 px-4 border-b border-gray-300">
    <div class="space-y-1 pt-2 pb-2 border-b border-gray-300">
      <a href="/listings" class="block rounded-md px-3 py-2 text-base font-medium text-gray-700 hover:bg-gray-300">Explore</a>
      <a href="/listings/new" class="block rounded-md px-3 py-2 text-base font-medium text-gray-700 hover:bg-gray-300">Add new Listing</a>
    </div>
    <div class="space-y-1 pt-2">
      <% if(!currentUser){ %>
        <a href="/signup" class="block rounded-md px-3 py-2 text-base font-medium text-gray-700 hover:bg-gray-300">Sign Up</a>
        <a href="/login" class="block rounded-md px-3 py-2 text-base font-medium text-gray-700 hover:bg-gray-300">Log In</a>
      <% } if(currentUser){ %>
        <a href="/logout" class="block rounded-md px-3 py-2 text-base font-medium text-gray-700 hover:bg-gray-300">Log Out</a>
      <% } %>
    </div>
  </div>
</nav>

<script>
  const menuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const hamburgerIcon = document.getElementById('hamburger-icon');
  const crossIcon = document.getElementById('cross-icon');

  menuBtn.addEventListener('click', () => {
    mobileMenu.classList.toggle('hidden');
    hamburgerIcon.classList.toggle('hidden');
    hamburgerIcon.classList.toggle('block');
    crossIcon.classList.toggle('hidden');
    crossIcon.classList.toggle('block');
  });
</script>